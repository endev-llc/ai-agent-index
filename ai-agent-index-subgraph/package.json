{
  "name": "ai-agent-index-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ai-agent-index",
    "create-local": "graph create --node http://localhost:8020/ ai-agent-index",
    "remove-local": "graph remove --node http://localhost:8020/ ai-agent-index",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ai-agent-index"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.95.0",
    "@graphprotocol/graph-ts": "^0.37.0"
  }
}
